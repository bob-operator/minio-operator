---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    "helm.sh/hook": pre-install,pre-upgrade
    "helm.sh/hook-weight": "-5"
    controller-gen.kubebuilder.io/version: v0.9.0
  name: miniojobs.minio.bob.com
spec:
  group: minio.bob.com
  names:
    kind: MinIOJob
    listKind: MinIOJobList
    plural: miniojobs
    singular: miniojob
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MinIOJob is the Schema for the miniojobs API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: MinIOJobSpec defines the desired state of MinIOJob
            properties:
              action:
                type: string
              minioRef:
                type: string
            required:
            - action
            - minioRef
            type: object
          status:
            description: MinIOJobStatus defines the observed state of MinIOJob
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
